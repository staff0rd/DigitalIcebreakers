{
  "rules": {
    "lobbys": {
      "$id": {
        ".write": "newData.child('ownerId').val() === auth.uid"
      }
    },
    "players": {
      "$id": {
        ".write": "$id === auth.uid",
        ".read": "$id === auth.uid"
      }
    }
  }
}
