server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: client-side routes like /game and /:lobbyCode
    location / {
        try_files $uri /index.html;
    }
}
