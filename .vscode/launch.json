{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Dev (vite + emulators)",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev",
      "cwd": "${workspaceFolder}/web"
    }
  ]
}
